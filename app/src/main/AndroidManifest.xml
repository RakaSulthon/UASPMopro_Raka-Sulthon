<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.restaurant.raka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name="com.restaurant.raka.CreateRestaurantActivity"></activity>
        <activity
            android:name="com.restaurant.raka.ProfileActivity"
            android:label="@string/profile_account" />
        <activity android:name="com.restaurant.raka.auth.ChangePasswordActivity" />
        <activity android:name="com.restaurant.raka.auth.RegisterActivity" />
        <activity android:name="com.restaurant.raka.auth.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.restaurant.raka.DashboardActivity" />
    </application>

</manifest>